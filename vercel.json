{
    "buildCommand": "cd savex-ui && npm run build",
    "outputDirectory": "savex-ui/.next",
    "framework": "nextjs",
    "rewrites": [
        {
            "source": "/api/:path*",
            "destination": "/api/:path*"
        }
    ],
    "env": {
        "NODE_ENV": "production"
    }
}